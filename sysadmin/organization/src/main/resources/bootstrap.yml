server:
  port: ${SERVER_PORT:8010}
spring:
  application:
    name: organization
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:150.158.169.207}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:150.158.169.207}:${REGISTER_PORT:8848}
        file-extension: yml
